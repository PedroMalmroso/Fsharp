List.filter
// List.filter takes 2 arguments. 
// 1) The first argument takes a an element of type 'T and returns a boolean.
// 2) The second argument Takes a list<'T> from which the elements are drawn.
// 3) List.filter returns only the elements in the list (2) for which are true in the boolean outcome of the function x.

List.foldBack
// List.foldBack takes 3 arguments.
